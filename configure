#!/bin/bash

export PREFIX=/usr
export BOOTDIR=/boot
export LIBDIR=$PREFIX/lib
export INCLUDEDIR=$PREFIX/include

export CC="i686-elf-gcc --sysroot=$PWD/sysroot"

export CFLAGS='-O2 -g'
export CPPFLAGS=''

export CC="$CC -isystem=$INCLUDEDIR"

echo $CC
